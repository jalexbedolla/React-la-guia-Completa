/*  Condicionales */

const disponible = 4000;
const retirar = 200;

if (disponible > retirar) {
  // se cumple la condicion, y se ejecuta el codigo
  console.log("si puedes retirar");
} else {
  // si no se cumple la condicion.. ejecuta este codigo
  console.log("fondo insuficiente");
}

/* estos son algunos de los operadores disponibles
 * > mayor que
 * < menor que
 * >= mayor o igual
 * <= menor o igual
 */
